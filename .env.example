# MarketLens environment configuration (copy to .env and fill in as needed)

# Optional: cache time-to-live for downloaded market data, in hours.
MARKETLENS_CACHE_TTL_HOURS=12

# Optional: API key for a news/sentiment provider. The core application works
# fully without this — the news module is optional.
NEWS_API_KEY=
